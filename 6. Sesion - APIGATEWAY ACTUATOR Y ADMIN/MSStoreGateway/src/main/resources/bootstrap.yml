spring:
  application:
    name: config-gateway
  cloud:
    config:
      uri:
      - http://localhost:8070
      username: jean
      password: 12345
  boot:
    admin:
      client:
        url: http://localhost:8099
        instance:
          prefer-ip: true